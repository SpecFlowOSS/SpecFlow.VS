<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net6.0</TargetFramework>
    <Packable>true</Packable>
		<GeneratePackageOnBuild>True</GeneratePackageOnBuild>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="SpecFlow" Version="3.9.40" />
  </ItemGroup>

  <!-- <Target Name="CustomAfterBuild" AfterTargets="Build" Condition="$(configuration)=='Release'"> -->
    <!-- <Message Text="Copying: $(ProjectDir)Deveroom.SampleSpecFlow3940.ExternalBindings.1.0.0.nupkg-&gt;'$(ProjectDir)..\'" Importance="high" /> -->

    <!-- <Copy SourceFiles="$(ProjectDir)Deveroom.SampleSpecFlow3940.ExternalBindings.1.0.0.nupkg" DestinationFiles="$(ProjectDir)..\Deveroom.SampleSpecFlow3940.ExternalBindings.1.0.0.nupkg" /> -->
  <!-- </Target> -->

</Project>
